<%= form_with(model: youtube_url, local: true) do |form| %>
  <% if youtube_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(youtube_url.errors.count, "error") %> prohibited this youtube_url from being saved:</h2>

      <ul>
        <% youtube_url.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
